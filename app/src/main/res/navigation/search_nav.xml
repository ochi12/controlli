<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/search_nav"
    app:startDestination="@id/mainActivityFragment">
    <activity
        android:id="@+id/mainActivityFragment"
        android:name="com.example.controlii.MainActivity1"
        android:label="activity_main"
        tools:layout="@layout/activity_main"/>
    <fragment
        android:id="@+id/searchView"
        android:name="com.example.controlii.searchView"
        android:label="fragment_search_view"
        tools:layout="@layout/fragment_search_view" >
        <action
            android:id="@+id/searchViewToMain"
            app:destination="@id/mainActivityFragment" />
    </fragment>
</navigation>